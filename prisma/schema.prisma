generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./imdb.db"
}

model Play {
  id          Int     @id @default(autoincrement())
  title       String
  premiere        Int
  director String
  description String?
}